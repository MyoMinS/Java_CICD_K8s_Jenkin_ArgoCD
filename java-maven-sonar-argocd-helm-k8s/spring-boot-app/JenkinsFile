pipeline {
    agent {
        docker { image 'myominsoe432/maven-docker-agent:v6' }
    }
    stages {
        stage('Build') {
            steps {
                script {
                    // Ensure Maven build runs in the container
                    sh 'mvn clean install'
                }
            }
        }
    }
}
